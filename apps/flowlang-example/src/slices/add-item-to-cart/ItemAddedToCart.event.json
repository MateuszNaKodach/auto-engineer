{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "ItemAddedToCart",
  "type": "object",
  "properties": {
    "cartId": {
      "type": "string",
      "description": "Unique identifier for the shopping cart"
    },
    "itemId": {
      "type": "string",
      "description": "Unique identifier for the item that was added"
    },
    "quantity": {
      "type": "integer",
      "minimum": 1,
      "description": "Quantity of the item that was added"
    },
    "price": {
      "type": "number",
      "minimum": 0,
      "description": "Price per unit of the item"
    },
    "totalItems": {
      "type": "integer",
      "minimum": 0,
      "description": "Total number of items in the cart after adding"
    }
  },
  "required": ["cartId", "itemId", "quantity", "price", "totalItems"],
  "additionalProperties": false
} 
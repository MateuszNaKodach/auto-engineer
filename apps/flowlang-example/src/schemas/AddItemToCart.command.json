{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "AddItemToCart",
  "type": "object",
  "properties": {
    "cartId": {
      "type": "string",
      "description": "Unique identifier for the shopping cart"
    },
    "itemId": {
      "type": "string",
      "description": "Unique identifier for the item to add"
    },
    "quantity": {
      "type": "integer",
      "minimum": 1,
      "description": "Quantity of the item to add"
    },
    "price": {
      "type": "number",
      "minimum": 0,
      "description": "Price per unit of the item"
    }
  },
  "required": ["cartId", "itemId", "quantity", "price"],
  "additionalProperties": false
} 